{
  "name": "subtitle-agent",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "mastra dev",
    "build": "mastra build",
    "start": "mastra start",
    "deploy": "mastra build && wrangler deploy --config .mastra/output/wrangler.json",
    "deploy:preview": "mastra build && wrangler deploy --config .mastra/output/wrangler.json --env preview",
    "wrangler:dev": "mastra build && wrangler dev --config .mastra/output/wrangler.json",
    "wrangler:login": "wrangler login",
    "secrets:setup": "echo 'Run the following commands to set up your secrets:' && echo 'wrangler secret put CLOUDFLARE_API_TOKEN' && echo 'wrangler secret put CLOUDFLARE_API_EMAIL' && echo 'wrangler secret put CLOUDFLARE_ACCOUNT_ID' && echo 'wrangler secret put OPENAI_API_KEY' && echo 'wrangler secret put YOUTUBE_API_KEY'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.0.0",
    "@mastra/core": "^0.13.1",
    "@mastra/deployer-cloudflare": "^0.12.0",
    "@mastra/loggers": "^0.10.6",
    "google-translate-api-x": "^10.7.2",
    "hono": "^4.9.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "mastra": "^0.10.20",
    "typescript": "^5.9.2",
    "wrangler": "^3.111.0"
  }
}
